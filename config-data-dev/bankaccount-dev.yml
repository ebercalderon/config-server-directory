bankaccount:
  property: BankAccount Profile Dev
server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  data:
    mongodb:
      url: mongodb+srv://dev:az9Y7tr5eGTgHzVF@nttdata.tipkdvn.mongodb.net/bootcamp?retryWrites=true&w=majority
      database: bootcamp
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  server:
    wait-time-in-ms-when-sync-empty: 0